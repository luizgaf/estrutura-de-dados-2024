#include <stdio.h>
#include <stdlib.h>
#include "lc.h"

int main(int argc, char* argv[]){
    No* H = no('A', no('B', no('C', no('D', no('E', no('F', no(No* L)))))));

    char valor = argv;
    imprimir_lista(H);
    inserir_no
}